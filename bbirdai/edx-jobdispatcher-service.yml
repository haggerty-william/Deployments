apiVersion: v1
kind: Service
metadata:
  name: edx-jobdispatcher
  namespace: bbirdai
spec:
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
  selector:
    app: edx-jobdispatcher