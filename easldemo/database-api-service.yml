apiVersion: v1
kind: Service
metadata:
  name: database-api
  namespace: easl-demo
spec:
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
  selector:
    app: database-api