apiVersion: v1
kind: Service
metadata:
  name: logging-api
  namespace: easl-demo
spec:
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
  selector:
    app: logging-api