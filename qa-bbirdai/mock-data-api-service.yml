apiVersion: v1
kind: Service
metadata:
  name: mock-data-api
  namespace: qa-bbirdai
spec:
   type: ClusterIP
   ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
   selector:
      app: mock-data-api